name: Probe label permission

on:
  workflow_dispatch:

permissions:
  issues: write

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: gh label create probelabel --force --repo latest-debs/apt-repo
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}